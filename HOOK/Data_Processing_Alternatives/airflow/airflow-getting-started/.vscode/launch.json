{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Airflow DAG",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/dags"
            },
            "justMyCode": false
        }
    ]
}